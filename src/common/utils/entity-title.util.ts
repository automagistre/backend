/**
 * Справочник подписей сущностей для отображения (аудит, журналы, контекст проводок).
 * Единая точка формирования человекочитаемых заголовков по простым полям сущности.
 * Если для подписи нужны данные из БД — используйте DisplayContextService.
 */

/** Подпись заказа: «Заказ №123». */
export function orderTitle(number: number | string): string {
  return `Заказ №${number}`;
}
